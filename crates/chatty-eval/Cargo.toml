[package]
name = "chatty-eval"
version.workspace = true
edition.workspace = true
description = "Dev-only evaluation harness: datasets, paired statistics, ablation flags (AGE-24)"
publish = false
rust-version = "1.85"

[lib]
name = "chatty_eval"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
rand = "0.8"
rand_chacha = "0.3"
statrs = "0.18"

[dev-dependencies]
tempfile = { workspace = true }
