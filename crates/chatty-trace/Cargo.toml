[package]
name = "chatty-trace"
version.workspace = true
edition.workspace = true
description = "Trace capture, ATIF round-trip, FeedbackFn (AGE-5) — ships in the app"
publish = false
rust-version = "1.94"

[lib]
name = "chatty_trace"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
