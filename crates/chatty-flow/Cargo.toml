[package]
name = "chatty-flow"
version.workspace = true
edition.workspace = true
description = "AFlow WorkflowRepr + IrRepr interpreter (AGE-13) — ships in the app"
publish = false
rust-version = "1.94"

[lib]
name = "chatty_flow"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
